/**
 * Create a new Set from an array
 *
 * @pamra {object} - An array of any kind
 * of elements.
 *
 * @returns A new set composed of items
 * from the given array.
 */
const setFromArray = (arr) => (
  new Set(arr)
);

export default setFromArray;
